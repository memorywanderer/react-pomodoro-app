export const SECOND = 1_000
export const MINUTE = SECOND * 60
export const INTERVAL = SECOND
export const DEFAULT_SESSION_TIME = MINUTE * 25;
export const DEFAULT_BREAK_TIME = MINUTE * 5;
export const MIN_TIME = MINUTE * 5;
export const MAX_TIME = MINUTE * 60;